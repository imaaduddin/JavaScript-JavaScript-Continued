// innerHTML

const h1 = document.querySelector("h1");
h1.innerHTML = "YOOOO!";
console.log(h1); // innerHTML can be used to change the element on the html file you selected 

const ul = document.querySelector("ul");
ul.innerHTML;
console.log(ul); // Over here innerHTML is displaying everything inside the ul element 
