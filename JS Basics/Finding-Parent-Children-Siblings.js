const firstLi = document.querySelector("li");
firstLi.parentElement; // This will return the parent element of the li you selected 

const selectUl = document.querySelector("ul");
selectUl.children; // this will return all the children elements inside the ul element 