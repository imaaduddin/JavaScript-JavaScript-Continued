// Function Expressions 

// There's another syntax we can use to define functions:

const square = function (num) {
    return num * num;
}

square(7); // 49

// Both functions below behave the exact same way 
function add(x, y) {
    return x * y;
}

const add = fucntion (x, y) {
    return x + y;
}